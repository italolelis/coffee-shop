# requirements.yaml
dependencies:
  - name: rabbitmq-ha
    version: "~1.18.0"
    repository: "https://kubernetes-charts.storage.googleapis.com"
    alias: "rabbitmq"
  - name: postgresql
    version: "~3.9.1"
    repository: "https://kubernetes-charts.storage.googleapis.com"
    condition: postgresql.enabled
  - name: nginx-ingress
    version: "~0.9.4"
    repository: "https://kubernetes-charts.storage.googleapis.com"
