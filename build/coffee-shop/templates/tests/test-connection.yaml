apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "coffee-shop.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "coffee-shop.name" . }}
    helm.sh/chart: {{ include "coffee-shop.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "coffee-shop.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
